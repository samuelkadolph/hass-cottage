harmony_sync_all:
  alias: Harmony - Sync All
  icon: mdi:remote
  mode: restart
  sequence:
  - action: harmony.sync
    target:
      entity_id: '{{ states.remote | map(attribute="entity_id") | list }}'
